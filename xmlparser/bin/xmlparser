#! /usr/local/bin/node

const fs       = require("fs");
const parseXML = require("./../lib/xml_parser").parseXML;

fs.readFile(process.argv[2], "utf8", (err, data) => {
    console.log(parseXML(data));
});
